from ..type import ExpandResult
from .base_structured_output_extractor import BaseStructuredOutputExtractor


class ExpandGenerator(BaseStructuredOutputExtractor):
    """扩展生成器"""

    OUTPUT_SCHEMA = ExpandResult
    SYSTEM_PROMPT = """\
        你是一个具有深度战略眼光的任务规划专家。我们正在通过蒙特卡洛树搜索来解决一个复杂问题。

        **原始用户问题：**
        <<<
        {user_input_content}
        >>>

        **当前处理过程的上下文摘要：**
        <<<
        {nodes_context}
        >>>

        **任务要求：**
        基于上述用户问题和处理过程的上下文摘要，请规划 **下一步** 可能采取的行动。
        
        为了保证搜索的广度，你需要生成 **0 到 3 个截然不同** 的候选方案。

        以下是生成的方案的风格参考：

        1. **（保守/直接）**：利用当前信息，最合乎逻辑的直接下一步。
        2. **（探索/验证）**：尝试查找更多信息，或者验证某个假设。
        3. **（迂回/创新）**：如果前路看似受阻，尝试换一种工具或思路。

        **注意事项：**
        - 如果你认为根据当前信息，任务已经 **彻底完成**，请返回 **空列表**。
        - 如果当前已经进入死胡同或判断任务已无法完成，无法继续，无继续意义，也返回 **空列表**。
        - 每次只规划 **接下来的一步行动**，不要生成多步计划。
        - 并不是需要每次都生成 2 个或 3 个，你需要自行判断，本次生成几个？生成的方案使用哪种方案风格？不同的方案风格也可以不同。

        请注意，按照要求返回相关的内容，不要输出错误的格式，不要输出错误的内容，不要包含任何额外的解释或文本。
        """
