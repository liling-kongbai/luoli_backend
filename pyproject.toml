[project]
name = "luoli-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.12"
dependencies = [
    "langgraph>=1.0.10",
]

[tool.ruff.format]
quote-style = "single"
